version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm install
      - npm run build:client
run:
  runtime-version: nodejs18
  command: npm start
  network:
    port: 3000
    env: PORT
  env:
    - name: NODE_ENV
      value: production