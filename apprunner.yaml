version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - npm install
      - npm run webpack -- --mode production
run:
  runtime-version: nodejs16
  command: npm start
  network:
    port: 3000
    env: PORT
  env:
    - name: NODE_ENV
      value: production